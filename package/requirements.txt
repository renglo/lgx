# LGX extension — runtime dependencies

langgraph>=0.2.0
openai>=1.0.0

# Renglo library (required for integration with schedulers, sessions, data)
# Install via: pip install -e ../../../dev/renglo-lib
